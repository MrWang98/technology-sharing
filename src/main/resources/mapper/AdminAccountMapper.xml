<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.technologysharing.mapper.AdminAccountMapper">
    <select id="selectByAdminName" resultType="com.example.technologysharing.pojo.AdminAccount">
        SELECT * FROM admin_account WHERE name=#{name}
    </select>

</mapper>